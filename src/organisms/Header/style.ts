import styled from "styled-components";

export const Box = styled.header`
    padding-top: 30px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;

    > img {
        width: 50px;
    }
`;